{
  "name": "delivery_app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "web:dev": "turbo run dev --filter=@foodii/web",
    "web:build": "turbo run build --filter=@foodii/web",
    "web:start": "turbo run start --filter=@foodii/web",
    "restaurateur:dev": "turbo run dev --filter=@foodii/restaurateur",
    "restaurateur:build": "turbo run build --filter=@foodii/restaurateur",
    "restaurateur:start": "turbo run start --filter=@foodii/restaurateur",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/charlesloubao/food_delivery.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/charlesloubao/food_delivery/issues"
  },
  "homepage": "https://github.com/charlesloubao/food_delivery#readme",
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
